const path = require("path");

module.exports = {
  config: path.resolve("src", "database", "config", "config.js"),
  "models-path": path.resolve("src", "database", "db", "models"),
  "seeders-path": path.resolve("src", "database", "db", "seeders"),
  "migrations-path": path.resolve("src", "database", "db", "migrations"),
};
